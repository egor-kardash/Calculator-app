import styled from 'styled-components';

import { baseTheme } from '@/theme';

export const PageLayout = styled.div`
  width: ${baseTheme.width.layout};
  height: ${baseTheme.height.layout};
  padding: ${baseTheme.padding.layout};
  margin: ${baseTheme.margin.layout};
`;
